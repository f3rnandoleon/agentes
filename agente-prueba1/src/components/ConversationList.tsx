"use client";

export default function ConversationList({items,selected,onSelect}:{items:any[];selected:number|null;onSelect:(id:number)=>void}){
  return <aside className="flex w-[22rem] shrink-0 flex-col border-r border-slate-200/80 bg-white">
    <div className="border-b border-slate-100 px-5 py-5"><div className="flex items-center justify-between"><div><h2 className="font-bold text-ink">Conversaciones</h2><p className="mt-0.5 text-xs text-muted-ink">Bandeja de entrada</p></div><span className="grid h-7 min-w-7 place-items-center rounded-full bg-cyan-50 px-2 text-xs font-bold text-ocean">{items.length}</span></div><div className="mt-4 flex items-center gap-2 rounded-xl bg-slate-100 px-3 py-2 text-sm text-slate-400"><span>⌕</span><span>Buscar conversación</span></div></div>
    <div className="scrollbar flex-1 overflow-y-auto p-2.5">{items.map(c=><button key={c.id} onClick={()=>onSelect(c.id)} className={`mb-1 w-full rounded-xl p-3 text-left transition ${selected===c.id?"bg-navy text-white shadow-lg shadow-slate-300":"hover:bg-slate-50"}`}><div className="flex items-start gap-3"><div className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl text-sm font-bold ${selected===c.id?"bg-white/15 text-white":"bg-cyan-50 text-ocean"}`}>{(c.name||c.phone||"?").slice(0,1).toUpperCase()}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><span className="truncate text-sm font-bold">{c.name||c.phone}</span><span className={`rounded-full px-2 py-0.5 text-[10px] font-bold ${selected===c.id?"bg-white/15 text-white":c.mode==="AI"?"bg-cyan-50 text-ocean":"bg-orange-50 text-coral"}`}>{c.mode}</span></div><p className={`mt-1 truncate text-xs ${selected===c.id?"text-slate-300":"text-muted-ink"}`}>{c.last_message_preview||"Sin mensajes"}</p></div></div></button>)}{items.length===0&&<div className="px-4 py-12 text-center text-sm text-muted-ink">Aún no hay conversaciones.</div>}</div>
  </aside>
}
