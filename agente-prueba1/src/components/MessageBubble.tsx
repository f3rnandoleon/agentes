export default function MessageBubble({message}:{message:{role:string;content:string;wa_message_id:string|null}}){const right=message.role!=="user";return <div className={`flex ${right?"justify-end":"justify-start"}`}><div className={`max-w-[75%] rounded-2xl px-4 py-2 text-sm shadow-sm ${message.role==="assistant"?"bg-emerald-100":""} ${message.role==="human"?"bg-amber-100":""} ${message.role==="user"?"bg-white":""}`}>{message.content}{right&&!message.wa_message_id&&<span className="ml-2 text-red-600" title="No enviado">⚠</span>}</div></div>}
